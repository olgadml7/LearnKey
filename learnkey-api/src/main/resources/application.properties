spring.application.name=learnkey-api

# ⚙️ Conexión a MySQL (Docker)
spring.datasource.url=jdbc:mysql://localhost:3307/learnKeyDB?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root  # o la que definiste en docker-compose
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 🧠 Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 🌐 Puerto del servidor (para evitar choque con phpMyAdmin)
server.port=8085
